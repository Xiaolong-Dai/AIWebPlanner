# 📦 作业提交完整指南

> 本文档将指导你完成作业提交的所有步骤，包括 GitHub Actions 配置、Docker 镜像构建和 PDF 文档生成。

---

## 📋 目录

1. [提交要求](#提交要求)
2. [准备工作](#准备工作)
3. [配置阿里云镜像仓库](#配置阿里云镜像仓库)
4. [配置 GitHub Actions](#配置-github-actions)
5. [构建和推送 Docker 镜像](#构建和推送-docker-镜像)
6. [生成提交 PDF](#生成提交-pdf)
7. [验证和测试](#验证和测试)

---

## 📝 提交要求

### 需要提交的内容

1. ✅ **PDF 文件**
   - 包含 GitHub 仓库地址
   - 包含完整的 README 文档
   - 包含 Docker 镜像下载和运行说明
   - 包含 API Key (如果不使用助教提供的)

2. ✅ **GitHub 仓库**
   - 完整的项目代码
   - 详细的提交记录
   - 完善的 README 文档

3. ✅ **Docker 镜像**
   - 推送到阿里云镜像仓库
   - 可以直接下载运行
   - 包含运行说明

---

## 🔧 准备工作

### 1. 注册阿里云账号

如果还没有阿里云账号：

1. 访问: https://www.aliyun.com
2. 点击 "免费注册"
3. 完成注册流程
4. 实名认证 (必需)

### 2. 开通容器镜像服务

1. 访问: https://cr.console.aliyun.com
2. 首次使用需要开通服务
3. 选择 "个人实例" (免费)
4. 设置 Registry 登录密码 (记住这个密码!)

---

## 🐳 配置阿里云镜像仓库

### 步骤 1: 创建命名空间

1. 访问: https://cr.console.aliyun.com/cn-hangzhou/instances
2. 点击左侧 "命名空间"
3. 点击 "创建命名空间"
4. 填写:
   - **命名空间名称**: `ai-web-planner` (或你喜欢的名称)
   - **自动创建仓库**: 选择 "私有"
5. 点击 "确定"

### 步骤 2: 创建镜像仓库

1. 点击左侧 "镜像仓库"
2. 点击 "创建镜像仓库"
3. 填写:
   - **命名空间**: 选择刚创建的 `ai-web-planner`
   - **仓库名称**: `frontend` (前端应用)
   - **仓库类型**: 选择 "公开" (方便助教下载)
   - **摘要**: `AI Web Planner 前端应用`
4. 点击 "下一步"
5. **代码源**: 选择 "本地仓库"
6. 点击 "创建镜像仓库"

**重复以上步骤创建后端仓库**:
- **仓库名称**: `backend`
- **摘要**: `AI Web Planner 后端代理服务`

### 步骤 3: 获取仓库信息

创建完成后，你会得到以下信息:

```
仓库地址 (公网):
registry.cn-hangzhou.aliyuncs.com/ai-web-planner/frontend
registry.cn-hangzhou.aliyuncs.com/ai-web-planner/backend

登录命令:
docker login --username=你的阿里云账号 registry.cn-hangzhou.aliyuncs.com
```

**记录以下信息** (后面会用到):
- ✅ Registry 地址: `registry.cn-hangzhou.aliyuncs.com`
- ✅ 命名空间: `ai-web-planner`
- ✅ 阿里云账号用户名
- ✅ Registry 登录密码

---

## ⚙️ 配置 GitHub Actions

### 步骤 1: 创建 GitHub Secrets

1. 打开你的 GitHub 仓库
2. 点击 **Settings** (设置)
3. 点击左侧 **Secrets and variables** → **Actions**
4. 点击 **New repository secret**

**添加以下 Secrets**:

| Name | Value | 说明 |
|------|-------|------|
| `ALIYUN_REGISTRY` | `registry.cn-hangzhou.aliyuncs.com` | 阿里云镜像仓库地址 |
| `ALIYUN_NAMESPACE` | `ai-web-planner` | 你的命名空间 |
| `ALIYUN_USERNAME` | 你的阿里云账号 | 登录用户名 |
| `ALIYUN_PASSWORD` | Registry登录密码 | 容器镜像服务密码 |

**添加方法**:
1. 点击 "New repository secret"
2. Name: 输入上表中的 Name
3. Secret: 输入对应的 Value
4. 点击 "Add secret"
5. 重复以上步骤添加所有 Secrets

### 步骤 2: 创建 GitHub Actions 工作流

在本地项目中创建以下文件结构:

```
.github/
└── workflows/
    └── docker-build.yml
```

**执行以下命令**:

```bash
# 创建目录
mkdir -p .github/workflows

# 创建工作流文件 (下一步会填充内容)
```

---

## 🚀 构建和推送 Docker 镜像

### 方式一: 使用 GitHub Actions (推荐)

#### 1. 工作流文件已创建

工作流文件 `.github/workflows/docker-build.yml` 已经为你准备好了。

#### 2. 触发构建

**自动触发** (推荐):
```bash
# 提交代码到 main 分支
git add .
git commit -m "feat: 添加 GitHub Actions 自动构建"
git push origin main
```

**手动触发**:
1. 访问 GitHub 仓库
2. 点击 **Actions** 标签
3. 点击左侧 "Build and Push Docker Images"
4. 点击右侧 "Run workflow"
5. 选择分支 (main)
6. 点击 "Run workflow"

#### 3. 查看构建进度

1. 在 **Actions** 标签页
2. 点击最新的工作流运行
3. 查看构建日志
4. 等待构建完成 (约 5-10 分钟)

#### 4. 验证镜像

构建成功后:
1. 访问阿里云容器镜像服务控制台
2. 点击 "镜像仓库"
3. 查看 `frontend` 和 `backend` 仓库
4. 应该能看到新推送的镜像标签 (如 `latest`, `v1.0.0`)

---

### 方式二: 本地手动构建 (备用)

如果 GitHub Actions 遇到问题，可以手动构建:

#### 1. 登录阿里云镜像仓库

```bash
docker login --username=你的阿里云账号 registry.cn-hangzhou.aliyuncs.com
# 输入 Registry 登录密码
```

#### 2. 构建前端镜像

```bash
cd frontend
docker build -t registry.cn-hangzhou.aliyuncs.com/ai-web-planner/frontend:latest .
docker push registry.cn-hangzhou.aliyuncs.com/ai-web-planner/frontend:latest
cd ..
```

#### 3. 构建后端镜像

```bash
cd backend
docker build -t registry.cn-hangzhou.aliyuncs.com/ai-web-planner/backend:latest .
docker push registry.cn-hangzhou.aliyuncs.com/ai-web-planner/backend:latest
cd ..
```

#### 4. 验证推送

```bash
# 查看本地镜像
docker images | grep ai-web-planner

# 在阿里云控制台查看
# https://cr.console.aliyun.com
```

---

## 📄 生成提交 PDF

### 步骤 1: 准备 README 内容

确保你的 `README.md` 包含以下内容:

- ✅ 项目简介
- ✅ 技术栈
- ✅ 功能特性
- ✅ Docker 镜像下载和运行说明
- ✅ API Key 配置说明
- ✅ 项目结构
- ✅ 演示截图

### 步骤 2: 创建 PDF 提交文档

我已经为你准备了一个 Markdown 模板，你需要:

1. 打开 `docs/SUBMISSION_DOCUMENT.md`
2. 填写你的个人信息
3. 填写 Docker 镜像地址
4. 添加项目截图

### 步骤 3: 转换为 PDF

**方式一: 使用在线工具 (推荐)**

1. 访问: https://www.markdowntopdf.com
2. 上传 `docs/SUBMISSION_DOCUMENT.md`
3. 点击 "Convert"
4. 下载生成的 PDF

**方式二: 使用 VS Code**

1. 安装插件: "Markdown PDF"
2. 打开 `docs/SUBMISSION_DOCUMENT.md`
3. 按 `Ctrl+Shift+P` (Windows) 或 `Cmd+Shift+P` (Mac)
4. 输入 "Markdown PDF: Export (pdf)"
5. 等待生成完成

**方式三: 使用 Typora**

1. 下载安装 Typora: https://typora.io
2. 打开 `docs/SUBMISSION_DOCUMENT.md`
3. 点击 "文件" → "导出" → "PDF"
4. 保存 PDF 文件

---

## ✅ 验证和测试

### 1. 验证 GitHub 仓库

- [ ] 代码已推送到 GitHub
- [ ] README.md 完整且格式正确
- [ ] 有详细的提交记录 (至少 10+ 次提交)
- [ ] 所有文档都已更新

### 2. 验证 Docker 镜像

**测试拉取镜像**:

```bash
# 拉取前端镜像
docker pull registry.cn-hangzhou.aliyuncs.com/ai-web-planner/frontend:latest

# 拉取后端镜像
docker pull registry.cn-hangzhou.aliyuncs.com/ai-web-planner/backend:latest

# 查看镜像
docker images | grep ai-web-planner
```

**测试运行**:

```bash
# 使用 docker-compose 运行
docker-compose up -d

# 访问应用
# 前端: http://localhost:3000
# 后端: http://localhost:3001/health

# 停止服务
docker-compose down
```

### 3. 验证 PDF 文档

- [ ] PDF 包含 GitHub 仓库地址
- [ ] PDF 包含完整 README 内容
- [ ] PDF 包含 Docker 镜像地址和运行说明
- [ ] PDF 包含 API Key (如需要)
- [ ] PDF 格式正确，可读性好

---

## 📊 提交清单

在提交前，请确认以下所有项目:

### GitHub 仓库
- [ ] 代码已推送到 GitHub
- [ ] 仓库地址: `https://github.com/你的用户名/AIWebPlanner`
- [ ] README.md 完整
- [ ] 有详细的提交记录
- [ ] 所有文档已更新

### Docker 镜像
- [ ] 镜像已推送到阿里云
- [ ] 前端镜像地址: `registry.cn-hangzhou.aliyuncs.com/你的命名空间/frontend:latest`
- [ ] 后端镜像地址: `registry.cn-hangzhou.aliyuncs.com/你的命名空间/backend:latest`
- [ ] 镜像可以正常拉取
- [ ] 镜像可以正常运行

### PDF 文档
- [ ] PDF 已生成
- [ ] 包含 GitHub 仓库地址
- [ ] 包含 README 内容
- [ ] 包含 Docker 运行说明
- [ ] 包含 API Key (如需要)
- [ ] 格式正确，可读性好

### API Key
- [ ] 如使用自己的 Key，已添加到 PDF
- [ ] Key 保证 3 个月内有效
- [ ] 已测试 Key 可用

---

## 🎯 最终提交

### 提交内容

1. **PDF 文件**: `AI-Web-Planner-提交文档.pdf`
2. **GitHub 仓库**: 在 PDF 中提供链接
3. **Docker 镜像**: 在 PDF 中提供拉取命令

### 提交方式

按照课程要求的方式提交 PDF 文件。

---

## ❓ 常见问题

### Q1: GitHub Actions 构建失败怎么办?

**A**: 
1. 检查 Secrets 是否正确配置
2. 查看 Actions 日志中的错误信息
3. 确认阿里云账号和密码正确
4. 如果还是失败，使用本地手动构建

### Q2: Docker 镜像太大怎么办?

**A**:
1. 项目已使用多阶段构建优化
2. 前端镜像约 50MB (nginx:alpine)
3. 后端镜像约 150MB (node:22-alpine)
4. 这是正常大小

### Q3: 如何验证镜像可以运行?

**A**:
```bash
# 拉取镜像
docker pull registry.cn-hangzhou.aliyuncs.com/你的命名空间/frontend:latest

# 运行测试
docker run -p 3000:80 registry.cn-hangzhou.aliyuncs.com/你的命名空间/frontend:latest

# 访问 http://localhost:3000
```

### Q4: PDF 转换后格式不对怎么办?

**A**:
1. 尝试不同的转换工具
2. 检查 Markdown 格式是否正确
3. 可以手动调整 PDF 格式
4. 或使用 Word 转 PDF

---

## 📞 获取帮助

如果遇到问题:

1. 查看本文档的常见问题部分
2. 查看 `docs/TROUBLESHOOTING.md`
3. 查看 GitHub Actions 日志
4. 查看阿里云控制台错误信息

---

**祝你提交顺利！** 🎉

---

**文档版本**: v1.0  
**最后更新**: 2025-11-02

