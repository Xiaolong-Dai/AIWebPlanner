# AI Web Planner - 项目提交文档

---

## 📋 基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | AI Web Planner - 智能旅行规划 Web 应用 |
| **学生姓名** | [请填写你的姓名] |
| **学号** | [请填写你的学号] |
| **提交日期** | 2025-11-02 |
| **GitHub 仓库** | https://github.com/Xiaolong-Dai/AIWebPlanner |

---

## 🔗 GitHub 仓库地址

### 仓库链接

```
https://github.com/Xiaolong-Dai/AIWebPlanner
```

### 访问方式

1. 点击上方链接访问 GitHub 仓库
2. 查看完整的项目代码和文档
3. 查看详细的提交记录

---

## 🐳 Docker 镜像信息

### 镜像仓库地址

**阿里云容器镜像服务 (公开仓库)**

| 服务 | 镜像地址 |
|------|----------|
| **前端应用** | `registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/frontend:latest` |
| **后端代理** | `registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/backend:latest` |

> **注意**: 请将 `[你的命名空间]` 替换为你在阿里云创建的实际命名空间名称

### 拉取镜像

```bash
# 拉取前端镜像
docker pull registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/frontend:latest

# 拉取后端镜像
docker pull registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/backend:latest
```

### 运行镜像

#### 方式一: 使用 Docker Compose (推荐)

1. **下载 docker-compose.yml**

访问 GitHub 仓库下载 `docker-compose.yml` 文件，或使用以下内容创建:

```yaml
services:
  # 后端代理服务
  backend:
    image: registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/backend:latest
    container_name: ai-web-planner-backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
    networks:
      - ai-planner-network

  # 前端应用
  frontend:
    image: registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/frontend:latest
    container_name: ai-web-planner-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - ai-planner-network

networks:
  ai-planner-network:
    driver: bridge
```

2. **启动服务**

```bash
# 启动所有服务
docker-compose up -d

# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

3. **访问应用**

- 前端应用: http://localhost:3000
- 后端 API: http://localhost:3001
- 健康检查: http://localhost:3001/health

4. **停止服务**

```bash
docker-compose down
```

#### 方式二: 单独运行容器

**运行后端**:
```bash
docker run -d \
  --name ai-planner-backend \
  -p 3001:3001 \
  -e NODE_ENV=production \
  registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/backend:latest
```

**运行前端**:
```bash
docker run -d \
  --name ai-planner-frontend \
  -p 3000:80 \
  registry.cn-hangzhou.aliyuncs.com/[你的命名空间]/frontend:latest
```

---

## 🔑 API 密钥配置

### 使用说明

本项目支持两种 API 密钥配置方式:

#### 方式一: 使用助教提供的阿里云百炼 Key (推荐)

如果使用助教提供的 Key，无需额外配置，直接在应用的设置页面输入即可。

#### 方式二: 使用自己的 API Key

如果使用自己的 API Key，请在应用首次运行后:

1. 访问 http://localhost:3000
2. 注册/登录账号
3. 点击右上角 "设置" 图标
4. 在 "API 配置" 页面填写以下信息

### 必需的 API Key

#### 1. Supabase (数据库 - 必需)

```
VITE_SUPABASE_URL=[请填写你的 Supabase URL]
VITE_SUPABASE_ANON_KEY=[请填写你的 Supabase Key]
```

**获取方式**:
1. 访问 https://supabase.com
2. 创建项目
3. Settings → API → 复制 URL 和 anon key

**有效期**: 永久有效

#### 2. 高德地图 (地图服务 - 必需)

```
VITE_AMAP_KEY=[请填写你的高德地图 Key]
```

**获取方式**:
1. 访问 https://lbs.amap.com
2. 创建应用
3. 添加 Web 端 Key

**有效期**: 永久有效

#### 3. 阿里云百炼 (AI 服务 - 推荐)

```
VITE_ALIYUN_LLM_API_KEY=[请填写你的阿里云 API Key]
VITE_ALIYUN_LLM_ENDPOINT=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
```

**获取方式**:
1. 访问 https://bailian.console.aliyun.com
2. 开通服务
3. 创建 API Key

**有效期**: 3 个月以上

### 可选的 API Key

#### 4. 科大讯飞 (语音识别 - 可选)

```
VITE_XFEI_APP_ID=[你的 APPID]
VITE_XFEI_API_KEY=[你的 APIKey]
VITE_XFEI_API_SECRET=[你的 APISecret]
```

**获取方式**:
1. 访问 https://www.xfyun.cn
2. 创建应用
3. 开通语音听写服务

---

## 📖 README 文档

以下是项目的完整 README 文档内容:

---

# AI Web Planner

智能旅行规划 Web 应用 - 通过 AI 理解用户需求，自动生成详细旅行路线和预算建议

## 📖 项目简介

AI Web Planner 是一款基于人工智能的智能旅行规划应用，旨在简化旅行规划流程，为用户提供个性化的旅行路线推荐和实时预算管理功能。

### 核心特性

- 🤖 **AI 智能规划**: 通过大语言模型理解用户需求，自动生成详细行程
- 🎤 **多模态输入**: 支持语音和文字输入，让规划更便捷
- 🗺️ **地图可视化**: 基于高德地图的行程可视化展示，支持真实路线规划
- 🚗 **智能交通规划**: 自动生成景点间交通方案，包含城际交通（航班/高铁）信息
- 💰 **预算管理**: 智能预算分配和实时费用追踪，支持语音录入费用
- ⚠️ **预算预警**: 三级预算预警系统（80%/90%/100%），及时提醒超支风险
- ☁️ **云端同步**: 基于 Supabase 的多设备数据同步
- 🔒 **安全可靠**: 完善的用户认证和数据隔离机制

## 🛠️ 技术栈

### 前端
- **框架**: React 19 + TypeScript
- **构建工具**: Vite 7
- **UI 组件库**: Ant Design 5
- **状态管理**: Zustand
- **路由**: React Router v6
- **HTTP 客户端**: Axios
- **图表**: Recharts
- **日期处理**: Day.js

### 后端服务
- **认证**: Supabase Auth
- **数据库**: Supabase PostgreSQL
- **实时同步**: Supabase Realtime

### 第三方服务
- **地图服务**: 高德地图 API
- **语音识别**: 科大讯飞语音识别 API
- **AI 模型**: 阿里云通义千问（百炼平台）

## 📁 项目结构

```
AI-Web-Planner/
├── frontend/                 # 前端应用
│   ├── src/
│   │   ├── components/      # 通用组件
│   │   ├── pages/           # 页面组件
│   │   ├── services/        # API 调用服务
│   │   ├── store/           # Zustand 状态管理
│   │   └── types/           # TypeScript 类型定义
│   ├── Dockerfile
│   └── package.json
├── backend/                 # 后端代理服务
│   ├── server.js
│   ├── Dockerfile
│   └── package.json
├── docs/                    # 文档
├── docker-compose.yml
└── README.md
```

## 🎯 功能特性

### 1. 智能行程规划
- AI 理解自然语言输入
- 自动生成详细行程安排
- 包含景点、餐厅、住宿推荐
- 智能交通路线规划

### 2. 地图可视化
- 行程在地图上可视化展示
- 支持路线规划和导航
- 景点标记和详情查看
- 实时路况信息

### 3. 预算管理
- 智能预算分配
- 实时费用追踪
- 支持语音录入费用
- 预算超支提醒
- 费用统计和分析

### 4. 多设备同步
- 基于 Supabase 的云端存储
- 多设备实时同步
- 离线数据缓存

## 🔒 安全说明

- ✅ 所有 API Keys 通过环境变量或 localStorage 管理
- ✅ 敏感文件已添加到 .gitignore
- ✅ 使用 Supabase RLS 实现数据隔离
- ✅ 密码使用 bcrypt 加密存储
- ✅ 所有请求通过 HTTPS 加密传输

## 📄 许可证

MIT License

---


